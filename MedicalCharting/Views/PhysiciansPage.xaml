<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="MedicalChartingMaui.Views.PhysiciansPage"
             Title="Physicians">
    <VerticalStackLayout Padding="16" Spacing="12">
        <Button Text="Add Physician" Command="{Binding AddCommand}" />
        <CollectionView ItemsSource="{Binding Physicians}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto" Padding="6">
                        <Label Text="{Binding Name}" />
                        <Button Grid.Column="1" Text="Delete"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}"
                                CommandParameter="{Binding Id}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
